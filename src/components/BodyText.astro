---
import type { Size } from 'types/common';

import Text from './Text.astro';

interface Props {
  as?: 'div' | 'p' | 'span';
  size?: Size;
}

const { as = 'span', size = 'md' } = Astro.props;
---

<Text as={as} variant="body" size={size}><slot /></Text>
