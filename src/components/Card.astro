---
import type { Padding } from 'types/common';

import styles from './Card.module.css';

interface Props {
  padding?: Padding;
}

const { padding } = Astro.props;
---

<div class:list={[styles.root, { [styles.paddingNone]: padding === 'none' }]}>
  <slot />
</div>
