---
import type { Size } from 'types/common';

import Text from './Text.astro';

interface Props {
  as?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6';
  size?: Size;
}

const { as = 'h1', size = 'md' } = Astro.props;
---

<Text as={as} variant="heading" size={size}>
  <slot />
</Text>
